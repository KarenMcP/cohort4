//define attributes / variables
    //number
    //There is a JavaScript function that returns the number format for a variable
    const functions = {
    
        size: (num) => {
            if (num < 10) return "small";
            if (num < 20) return "medium";
            return "large";
        },
    
        add: (num1, num2) => {
            return num1 + num2;
        },
    
        subtract: (num1, num2) => {
            return num1 - num2;
        },

        adding: (num1, num2) => {
            return num1 + num2;
        },
        
        string: (value1, value2) => {
            return value1 + value2 + " much fun";
        },

        Boo: (value) => {
            return value =="true"
        },

        HeyRay: (value) => {
            return Value1 = 1,2,3
        }

    };
    
    export default functions;
    

    //string
    //boolean
    //array
    //dictionary / objects
    //undefined
//sample if / else
//functions
    //parameters
    //returns
//arrays
    //add to the front
    //add to the end
    //update values
//loops 
    //for
    //for/in
    //while
    //do while
    //forEach (with array and function)
//Objects / Dictionaries
    //declare object
    //lookup key to retrieve value